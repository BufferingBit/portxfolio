<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact | Hero's Journey</title>
    <link rel="stylesheet" href="/styles.css">
    <link
        href="https://fonts.googleapis.com/css2?family=Cinzel:wght@500;700&family=Inter:wght@300;400;500;600&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <style>
        :root {
            --space: #0F0F1A;
            --gold: #FCD34D;
            --gold-light: rgba(252, 211, 77, 0.1);
            --gold-medium: rgba(252, 211, 77, 0.3);
            --gold-dark: rgba(252, 211, 77, 0.7);
            --steel: #4B5563;
            --light: #F3F4F6;
            --dimmed: #9CA3AF;
            --success: #10B981;
            --warning: #F59E0B;
            --danger: #EF4444;
        }

        body {
            background-color: var(--space);
            color: var(--light);
            font-family: 'Inter', sans-serif;
            line-height: 1.6;
        }

        .font-cinzel {
            font-family: 'Cinzel', serif;
        }

        .text-gold {
            color: var(--gold);
        }

        .text-dimmed {
            color: var(--dimmed);
        }

        .contact-header {
            background: linear-gradient(145deg, rgba(15, 15, 26, 0.7), rgba(31, 41, 55, 0.4));
            border: 1px solid var(--gold-light);
            backdrop-filter: blur(8px);
        }

        .contact-card {
            background: linear-gradient(145deg, rgba(15, 15, 26, 0.7), rgba(31, 41, 55, 0.4));
            border: 1px solid var(--gold-light);
            border-radius: 1rem;
            padding: 2rem;
            transition: all 0.3s ease;
            backdrop-filter: blur(8px);
        }

        .contact-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(15, 15, 26, 0.5);
            border-color: var(--gold-medium);
        }

        .contact-icon {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            background: var(--gold-light);
            color: var(--gold);
            margin-bottom: 1.5rem;
            transition: all 0.3s ease;
        }

        .contact-card:hover .contact-icon {
            background: var(--gold);
            color: var(--space);
            transform: scale(1.1);
        }

        .form-input {
            background: rgba(31, 41, 55, 0.4);
            border: 1px solid var(--gold-light);
            border-radius: 0.5rem;
            padding: 0.75rem 1rem;
            width: 100%;
            color: var(--light);
            transition: all 0.3s ease;
        }

        .form-input:focus {
            outline: none;
            border-color: var(--gold);
            box-shadow: 0 0 0 3px var(--gold-light);
        }

        .form-label {
            display: block;
            margin-bottom: 0.5rem;
            font-size: 0.875rem;
        }

        .submit-btn {
            display: inline-flex;
            align-items: center;
            padding: 0.75rem 1.75rem;
            background: linear-gradient(135deg, var(--gold), #F59E0B);
            border: none;
            border-radius: 0.75rem;
            font-size: 1rem;
            font-weight: 500;
            color: var(--space);
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(252, 211, 77, 0.4);
        }

        .submit-btn i {
            margin-left: 0.5rem;
            transition: transform 0.3s ease;
        }

        .submit-btn:hover i {
            transform: translateX(3px);
        }

        .contact-method {
            display: flex;
            align-items: center;
            padding: 0.75rem 1rem;
            background: rgba(31, 41, 55, 0.4);
            border-radius: 0.5rem;
            transition: all 0.3s ease;
        }

        .contact-method:hover {
            background: rgba(55, 65, 81, 0.5);
            transform: translateY(-3px);
        }

        .contact-method i {
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--gold-light);
            color: var(--gold);
            border-radius: 50%;
            margin-right: 1rem;
        }

        /* Animations */
        @keyframes float {

            0%,
            100% {
                transform: translateY(0);
            }

            50% {
                transform: translateY(-10px);
            }
        }

        .floating {
            animation: float 6s ease-in-out infinite;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .contact-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>

<body class="bg-space text-light">
    <%- include('../partials/header.ejs') %>

        <main>
            <section class="container mx-auto px-4 py-12">
                <div class="max-w-6xl mx-auto">
                    <!-- Header -->
                    <div class="contact-header rounded-2xl p-8 mb-12 text-center">
                        <h1 class="text-4xl md:text-5xl font-cinzel text-gold mb-4">
                            <i class="fas fa-envelope mr-3"></i>Send a Raven
                        </h1>
                        <p class="text-xl text-dimmed max-w-2xl mx-auto">
                            Have a quest that requires my skills? Send a message via raven and I'll respond with haste.
                        </p>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                        <!-- Contact Form -->
                        <div class="contact-card animate__animated animate__fadeInLeft">
                            <h2 class="text-2xl font-cinzel text-gold mb-6">Message Scroll</h2>

                            <form id="contactForm" class="space-y-5" onsubmit="sendMail(); return false">
                                <div>
                                    <label for="name" class="form-label">Your Name</label>
                                    <input type="text" id="name" name="name" class="form-input"
                                        placeholder="Enter your name" required>
                                </div>

                                <div>
                                    <label for="email" class="form-label">Your Email</label>
                                    <input type="email" id="email" name="email" class="form-input"
                                        placeholder="Enter your email" required>
                                </div>

                                <div>
                                    <label for="subject" class="form-label">Subject</label>
                                    <input type="text" id="subject" name="subject" class="form-input"
                                        placeholder="Quest proposition">
                                </div>

                                <div>
                                    <label for="message" class="form-label">Your Message</label>
                                    <textarea id="message" name="message" rows="5" class="form-input"
                                        placeholder="Describe your quest in detail..." required></textarea>
                                </div>

                                <div class="pt-2">
                                    <button type="submit" class="submit-btn">
                                        Send Message <i class="fas fa-paper-plane"></i>
                                    </button>
                                </div>
                            </form>
                        </div>

                        <!-- Contact Methods -->
                        <div class="space-y-6 animate__animated animate__fadeInRight">
                            <div class="contact-card h-full">
                                <div class="flex justify-center mb-6">
                                    <div class="contact-icon floating">
                                        <i class="fas fa-map-marker-alt"></i>
                                    </div>
                                </div>

                                <h2 class="text-2xl font-cinzel text-gold mb-6 text-center">Contact Coordinates</h2>

                                <div class="space-y-4">
                                    <a href="mailto:your.email@example.com" class="contact-method">
                                        <i class="fas fa-envelope"></i>
                                        <div>
                                            <div class="text-sm text-dimmed">Email</div>
                                            <div class="text-gold">buffering.bit@gmail.com</div>
                                        </div>
                                    </a>

                                    <a href="tel:+1234567890" class="contact-method">
                                        <i class="fas fa-phone-alt"></i>
                                        <div>
                                            <div class="text-sm text-dimmed">Phone</div>
                                            <div class="text-gold">+91 123XXXXXXX</div>
                                        </div>
                                    </a>

                                    <div class="contact-method">
                                        <i class="fas fa-globe"></i>
                                        <div>
                                            <div class="text-sm text-dimmed">Based in</div>
                                            <div class="text-gold">Muzaffarpur, Bihar</div>
                                        </div>
                                    </div>
                                </div>

                                <div class="mt-8">
                                    <h3 class="font-cinzel text-gold mb-4">Follow the Journey</h3>
                                    <div class="flex space-x-4">
                                        <a href="https://github.com/BufferingBit"
                                            class="w-10 h-10 flex items-center justify-center bg-steel rounded-full hover:bg-gold hover:text-space transition-all">
                                            <i class="fab fa-github"></i>
                                        </a>
                                        <a href="https://www.linkedin.com/in/dhruv-kumar-2a06b2229/"
                                            class="w-10 h-10 flex items-center justify-center bg-steel rounded-full hover:bg-blue-500 hover:text-white transition-all">
                                            <i class="fab fa-linkedin-in"></i>
                                        </a>
                                        <a href="https://x.com/BufferingBit"
                                            class="w-10 h-10 flex items-center justify-center bg-steel rounded-full hover:bg-blue-400 hover:text-white transition-all">
                                            <i class="fab fa-twitter"></i>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </section>
        </main>

        <%- include('../partials/footer.ejs') %>

            <script>
                document.addEventListener('DOMContentLoaded', () => {
                    // Form submission
                    const contactForm = document.getElementById('contactForm');

                    contactForm.addEventListener('submit', (e) => {
                        e.preventDefault();

                        // Get form values
                        const formData = new FormData(contactForm);
                        const data = Object.fromEntries(formData);

                        // Here you would typically send the data to a server
                        console.log('Form submitted:', data);

                        // Show success message
                        alert('Your message has been sent! I will respond to your raven soon.');
                        contactForm.reset();
                    });

                    // Add animation class on scroll
                    const animateOnScroll = () => {
                        const contactCards = document.querySelectorAll('.contact-card');
                        const windowHeight = window.innerHeight;

                        contactCards.forEach(card => {
                            const cardTop = card.getBoundingClientRect().top;

                            if (cardTop < windowHeight - 100) {
                                card.classList.add('animate__fadeInUp');
                            }
                        });
                    };

                    // Initial check
                    animateOnScroll();

                    // Check on scroll
                    window.addEventListener('scroll', animateOnScroll);
                });

                function sendMail() {
                    const name = document.getElementById('name').value;
                    const email = document.getElementById('email').value;
                    const subject = document.getElementById('subject').value || 'Quest Proposition';
                    const message = document.getElementById('message').value;

                    const body = `Dear Adventurer,\n\n${message}\n\n--\n${name}\n${email}`;

                    window.location.href = `mailto:your-email@domain.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;

                    // Optional: Show confirmation
                    setTimeout(() => {
                        alert("Your message scroll is ready! Check your email client to send it forth.");
                    }, 300);
                }
            </script>
</body>

</html>